# Imagen

Tauri + React + TypeScript desktop app for GPT Image 2 generation.

## Layout

- `src-tauri/` Rust backend
- `src/` React frontend
- `outputs/` generated images

## Next step

Install dependencies and run the app with Tauri.

